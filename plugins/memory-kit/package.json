{
  "name": "@hanhnd/memory-kit",
  "version": "1.0.0",
  "description": "Memory Kit - Native TypeScript persistent memory for agent-kit",
  "type": "module",
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hanh-nd/agent-kit.git"
  },
  "scripts": {
    "build:plugin": "node dist/utils/build-plugin.js",
    "prepublishOnly": "npm run build:plugin",
    "build": "rm -rf dist && tsc && npm run build:plugin",
    "test": "rm -rf dist && tsc && node --test $(find tests -name '*.test.ts' | sed 's#^#dist/#; s#\\.ts$#.js#' | tr '\\n' ' ')"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "typescript": "^5.8.3"
  }
}
