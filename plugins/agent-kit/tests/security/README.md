# Security Module Tests

## Running tests

```bash
npm test
```

## Coverage

Generate coverage with:

```bash
node --test --experimental-test-coverage 'tests/**/*.test.js'
```

Target thresholds: >= 90% line coverage, 100% branch coverage on each module's exported functions.
